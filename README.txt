Prerequisites :
Project assumes that nodes and appium are already installed in the system and all the respective environment variables like JAVA_HOME and ANDROID_HOME are set correctly on the system.

1. Project is a maven project. Import in eclipse as a maven project to download all dependencies.
2. Change the PLATFORM_VERSION, DEVICE_NAME and UDID in the TestClass file to your device values before running the script.
